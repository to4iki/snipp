- if signed_in?
  .row
    .span8
      %h2= @snippet.title
      %h6= @snippet.tag_list
      = raw show_markdown( @snippet.command )
      [description]
      .well= @snippet.description

- else
  ログインして下さい
